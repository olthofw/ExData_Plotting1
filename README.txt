Each file constructs the corresponding plot with the following steps:

1) Set the correct source 
2) Run the main function by typing main() in the command line
3) Choose the folder with the dataset as the working directory when asked
4) PNG-files are created in the directory selected at step 3